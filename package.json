{
  "name": "@signalk/signalk-node-red",
  "version": "2.7.0",
  "description": "Node-RED Plugin",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changelog": "github-changes -o signalk -r signalk-node-red -a --only-pulls --use-commit-body --data=pulls  --tag-name=v$npm_package_version",
    "release": "git tag -d v$npm_package_version; npm run changelog && git add CHANGELOG.md && git commit -m 'chore: update changelog' && git tag v$npm_package_version && git push --tags && git push"
  },
  "keywords": [
    "signalk-node-server-plugin",
    "signalk-webapp"
  ],
  "author": "Scott Bender",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/SignalK/signalk-node-red"
  },
  "dependencies": {
    "node-red": "^0.18.7",
    "@signalk/node-red-embedded": "^2.8.1",
    "lodash": "^4.17.4",
    "geodist": "^0.2.1",
    "compare-versions": "^3.0.1"
  },
  "devDependencies": {
    "github-changes": "^1.1.2"
  }
}
